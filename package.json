{
  "name": "nutriscan",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "install:all": "npm install && npm install --prefix client && npm install --prefix server",
    "build": "npm run build --prefix client",
    "heroku-postbuild": "npm install --prefix server --omit=dev && cd client && npm install --include=dev && npm run build && cd .. && mkdir -p server/public && cp -r client/dist/* server/public/",
    "start": "node server/src/index.js",
    "test": "npm test --prefix client && npm test --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
